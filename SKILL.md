---
name: elsa_sketch
description: 生成 elsa_sketch 涂鸦风格的简约手绘概念可视化图（信息图/隐喻图/模型图/对话图）。柔和配色、黑色手绘线稿、浅投影层次、简约小人常有着色躯干，图中文字默认中文手写体。触发语：用户说"elsa sketch""elsa风格""用elsa风格画一张图"即走本 skill；也用于"做概念图""可视化""手绘信息图""涂鸦解释图""把这段内容画成图""生成此风格配图"等任务。
---

# elsa_sketch 简约手绘概念可视化图

## 核心定位

把一段内容（一个观点、一组对比、一个流程、一个结构、一个隐喻）画成**一张清爽的 elsa_sketch 风格概念图**。与更粗犷的马克笔涂鸦风不同，elsa_sketch 的特点：
- 线条更克制、人物更修长、常有着色躯干；
- 配色柔和低饱和（鼠尾草绿、浅蓝、暖黄、柔橙、灰褐、浅灰）；
- 常用浅灰或淡色柔和投影增加层次，不扁平也不过度 3D；
- 会用对话气泡、镜子/屏幕/梯子等隐喻物件；
- 文字默认中文手写，隐喻类图默认可不加文字。

**重要：`assets/examples/` 里的 13 张原图只是风格参考，不是固定产出集。** 新图要以它们的画风为基准，从当前内容自由发明新构图，不要照抄参考图的布局、物件或英文标注。

## 风格锚点

来自 `assets/examples/`。核心 DNA 见 `references/style-dna.md`。

## 何时用本 skill

- **主触发语**：用户说"elsa sketch""elsa风格""用elsa风格画一张图"（无论后面是否带内容）即走本 skill。
- 用户说"把这段内容可视化为手绘图""生成概念图/对比图/脑图/信息图"
- 用户给了 Sketch - elsa 系列里的某张图，要求"照这个风格再画一张，文字换成中文"
- 用户明确"生成此风格视觉化图片"

不要把它当成商业插画、PPT 模版或可爱卡通。

## 先读这些参考

按任务需要读取，不要一次塞满上下文：

- `assets/examples/` + `INDEX.md`：13 张风格参考原图（**只作视觉校准，不照抄构图/英文标注**）。
- `references/style-dna.md`：风格 DNA、颜色、文字、禁忌。
- `references/composition-library.md`：13 种常见结构模板（可套用、可混搭、可超出）。
- `references/prompt-template.md`：单张生图提示词模板。
- `references/qa-checklist.md`：生成后检查和迭代规则。

## 工作流

### 1. 消化输入

读用户给的正文 / 链接 / 截图 / 一段想法。先判断**内容量**，再决定呈现策略：

**内容多（有结构、多要素、多关系）→ 结构化呈现**
- 用信息图、可视化、有结构的图把信息组织清楚，不让画面散。
- 优先从 `composition-library.md` 挑贴合的结构：隐喻/模型、左右二分、爆米花/放射、线性流程、对话、对比、象限/三维等。如果没有贴合的结构，也可以参考常用信息可视化呈现结构如：四宫格、飞轮、三角关系、多层级金字塔、黄金圈等。
- 信息多时可拆成多张图，每张只承载一个子结构，避免一张图塞爆。

**内容少（只有关键词、一句话、一个概念）→ 用隐喻呈现**
- 不要干巴巴写一行字，要从这个少量信息里**发明一个成立的小隐喻/场景**，让抽象概念变得可画、可感。
- 主人物必须参与这个隐喻场景的核心动作，画面要有"一想就懂"的巧劲。
- **隐喻图默认不加任何文字**（包括标题和标注）。除非用户主动要求文字，否则纯靠画面讲故事——文字会削弱隐喻的"一看就懂"。若用户要文字，再用中文手写体，且尽量精简。

提炼这张图要讲清的**唯一核心结构**：

- 是隐喻/抽象概念？（→ 举物、镜像、公式、AI 对话、问题）
- 是分类/对比？（→ 左右二分、发散收敛）
- 是流程/递进？（→ 线性流程、三阶段）
- 是模型/周期？（→ ZPD 脚手架、扩散曲线）
- 是多人关系/对话？（→ 对话气泡、三维空间）

一张图只讲一个结构（或承载一个隐喻）。不要贪多、不要散。

### 2. 选结构 + 写中文标注

从 `references/composition-library.md` 里挑最贴合的 1 个结构模板（也可混搭），然后把原图里的英文标注**逐条翻译成贴切的中文手写短词**，保持简短（2-8 字最佳）。

### 3. 写单张提示词

用 `references/prompt-template.md` 的模板组装提示词。模板已强制包含：纯白底、黑色手绘线稿、柔和配色、简约小人、中文手写体、禁英文、禁印刷体、禁 PPT。

不要照抄 `assets/examples/` 里参考图的构图、物件或英文标注，要从当前内容重新发明一个成立的小隐喻。结构模板只是常见起点，完全可以超出这 13 种去发明新结构。

### 4. 生成

用内置图像生成大模型，如 `ImageGen` 每张单独生成，不要把多张拼一张。

**尺寸规则（二选一，看本次指令）**：
- **默认（无特殊说明）**：微信公众号题图尺寸 **900×383（约 2.35:1 横版）**，提示词写明 `微信公众号题图尺寸 900x383`。
- **用户说明要做小红书图时**：采用小红书封面尺寸 **1080×1440（3:4 竖版）**，提示词写明 `小红书封面尺寸 1080x1440（3:4 竖版）`。
- 用户若有其它特殊比例（16:9、1:1、其他竖版）则以其指令为准。

判断要点：只要用户本次消息提到"小红书""xhs""小红书封面/题图"等，就走小红书尺寸；否则一律微信公众号题图尺寸。

### 5. 检查与迭代

对照 `references/qa-checklist.md`：
- 隐喻图出现文字 → 若用户未要求文字，重生成并加重"画面中禁止任何文字"
- 图里出现英文 → 重生成，提示词里加重"全部中文手写"
- 文字像印刷体 → 重生成，加重"手写钢笔/马克笔体"
- 背景不白 → 重生成，加重"纯白背景"
- 结构乱/太满 → 重生成或重选模板

### 6. 保存交付

若用户在 workspace 内工作，存到：

```text
assets/<topic-slug>-elsa-sketch/
01-<结构名>.png
02-<结构名>.png
```

保留原始生成文件，不覆盖已有资产，除非用户明确要求替换。

## 输出口径

交付时简短说明：生成了几张、每张讲什么结构/隐喻、保存路径、哪张最稳哪张是备选。让图自己说话，不堆风格理论。
